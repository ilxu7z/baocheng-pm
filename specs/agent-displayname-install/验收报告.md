# 验收报告 · Agent 显示名修复 + GitHub 安装链路现代化

- **验收人**：镜衡（shencha · 质控部）
- **验收日期**：2026-08-01
- **验收范围**：/home/chee/Projects/oc-macs「Agent 显示名修复 + GitHub 安装链路现代化」改动
- **契约约束**：C-001 — Agent ID(courtId)、openclaw.json 结构、install.sh 复用/增量机制保持不变
- **方式**：只读验收，未修改任何文件

---

## 结论摘要

**整体判定：✅ 通过（附 2 项轻微项，1 项建议核查）**

现代化改名目标已达成：registry.json 的 11 个 courtTitle 全为现代名，`main.name=鮱澄`，无古名；agents.json 覆盖全部 11 agent；install.sh/start.sh 的新注册逻辑均已写入 `name + identity.name( + identity.emoji)`；agents/ 目录所有子 agent bootstrap 标题均为现代名，`huizong` 已正确为「归藏」（全仓库无「汇综」残留）。语法校验 registry.json ✅ / start.sh ✅ / install.sh ✅ / add-agent.sh ✅。

**2 项需主 Agent 注意**（非阻塞，但建议处理）：
1. **ld-r 命名不一致**：registry.json 的 `ld-r.name = "Ld.r"`，而 agents.json 的 `ld-r.name = "执行经理"`。契约方案中 ld-r = 执行经理(执行办)。两文件名称不统一。
2. **agents.json 非纯 JSON**：文件含 `#` 注释行，`python3 -m json.tool agents.json` 直接失败（需先剔除注释行才可通过）。registry.json 本身纯 JSON 通过。

---

## 逐验收项明细

### 1. registry.json ✅ PASS

- **文件**：`registry.json`（11 条，`python3 -m json.tool` 通过）
- **11 个 courtTitle 全为现代名**（逐一核对）：

| id | name | courtId | courtTitle |
|----|------|---------|-----------|
| main | 鮱澄 | taizi | 总办 |
| ld-r | Ld.r | shangshu | 执行办 |
| guihua | 筹微 | zhongshu | 规划部 |
| shenyi | 审微 | menxia | 审议部 |
| paifa | 驿使 | libu_hr | 人力路由处 |
| wenan | 墨卿 | libu | 内容部 |
| daima | 锋铸 | bingbu | 开发部 |
| sheji | 绘象 | gongbu | 设计部 |
| shencha | 镜衡 | xingbu | 质控部 |
| huizong | 归藏 | hubu | 交付汇总处 |
| rongcui | 溶萃 | zaochao | 运维组 |

- **main.name = 鮱澄** ✅（L2）
- **无古名**（太子/中书省/尚书省/吏部/礼部/兵部/刑部/工部/户部/钦天/准奏/封驳/皇上/痨魍等）：registry.json 内 0 命中 ✅
- **契约 C-001 保持**：courtId 仍为 taizi/shangshu/zhongshu/menxia/libu_hr/libu/bingbu/gongbu/xingbu/hubu/zaochao —— 未改动 ✅

**⚠️ 发现项 D1（建议）**：`ld-r.name = "Ld.r"` 是拉丁字母名，非现代中文名「执行经理」（执行办）。契约方案明确 ld-r = 执行经理。虽不影响 courtTitle / id / 结构，但与方案及 agents.json 不一致。建议将 registry.json `ld-r.name` 同步为「执行经理」（或至少与 agents.json 统一）。

### 2. agents.json ✅ PASS（含 1 项轻微）

- **文件**：`agents.json`（11 条，含注释）
- **覆盖全部 11 agent（含 ld-r/rongcui/main）** ✅
- **名称与 registry 一致**：10/11 一致；唯一不一致见下方 ⚠️

| id | agents.json name | registry.json name | 一致? |
|----|------------------|--------------------|-------|
| guihua | 筹微 | 筹微 | ✅ |
| shenyi | 审微 | 审微 | ✅ |
| paifa | 驿使 | 驿使 | ✅ |
| wenan | 墨卿 | 墨卿 | ✅ |
| daima | 锋铸 | 锋铸 | ✅ |
| sheji | 绘象 | 绘象 | ✅ |
| shencha | 镜衡 | 镜衡 | ✅ |
| huizong | 归藏 | 归藏 | ✅ |
| **ld-r** | **执行经理** | **Ld.r** | **❌** |
| rongcui | 溶萃 | 溶萃 | ✅ |
| main | 鮱澄 | 鮱澄 | ✅ |

**⚠️ 发现项 D1（同 registry）：ld-r 名称不一致**。

**⚠️ 发现项 D2（轻微）**：agents.json 第 1-3 行为 `#` 注释行，导致 `python3 -m json.tool agents.json` 返回 `Expecting value: line 1` 失败。剔除注释后 JSON 有效（11 条）。文件头注释已在注释中说明「实际安装时 install.sh 从 registry.json 动态生成」，故该文件为参考模板，功能无碍，但严格 JSON 校验不通过。建议：若需脚本化校验，应去注释后校验；或改用 JSONC 工具。此项不阻塞功能。

### 3. install.sh ✅ PASS

- **register_agents 段**（L1491 起）：新注册 agent 时写入
  - `entry['name'] = ag.get('name', ag_id)` ✅
  - `entry['identity']['name'] = ag.get('name', ag_id)` ✅
  - `entry['identity']['emoji'] = ag.get('emoji', '🤖')` ✅
  - 证据：L1520-1525 `entry = {'id':..., 'workspace':..., 'name':..., 'identity': {'name':..., 'emoji':...}, 'subagents':...}`
  - **增量机制保持**：L1517 `existing_ids = {a['id'] for a in agents_list}` + `if ag_id not in existing_ids` 才注册，已存在 skip（L1530-1531）✅ —— 符合 C-001
- **bash -n install.sh** → ✅ 通过
- **main SOUL 模板**：「你是鲍澄（太子）」→ 已改为 **「你是鲍澄（总办）」**（L672）✅
  - 证据：`sed -n 660,675p` 显示 L672 `你是鲍澄（总办），八位 Agent 统领者。`
  - 另复查 L870：`你是谁？鲍澄 — 三省六部制调度中枢，八位 Agent 统领者。`（此处「三省六部」为体系名，可接受）

### 4. start.sh ✅ PASS

- **ensure_agents_registered**（L42 起）：新注册 agent 写入
  - `"name": ag.get("name", ag_id)`（L65）✅
  - `"identity": {"emoji": ag.get("emoji", "🤖"), "name": ag.get("name", ag_id)}`（L66）✅
- bash -n start.sh → ✅ 通过

### 5. agents/ 目录 ✅ PASS

- **huizong**：SOUL.md 第 1 行标题 `# SOUL.md · 归藏 (huizong)` ✅；AGENTS.md 第 1-2 行 `# AGENTS.md · 归藏 (huizong) 精简工作协议` ✅ —— 已为「归藏」非「汇综」
- **其余子 agent bootstrap 标题均为现代名**（逐一核对首行）：

| 目录 | SOUL.md 标题 |
|------|-------------|
| daima | 锋铸 |
| guihua | 筹微 |
| paifa | 驿使 |
| rongcui | 溶萃 |
| sheji | 绘象 |
| shencha | 镜衡 |
| shenyi | 审微 |
| wenan | 墨卿 |
| huizong | 归藏 |

- agents/ 目录古名扫描：**0 命中**（太子/中书省/门下省/尚书省/吏部/礼部/兵部/刑部/工部/户部/钦天/准奏/封驳/皇上/痨魍/汇综/储君/军机处/朝报官/中书令/尚书令/侍中）✅

### 6. 全仓库古名精扫（范围内）✅ PASS

范围：agents/ + scripts/*.py + install.sh + start.sh（+ 补充 add-agent.sh）

**见下文「古名清单一览」表。结论：范围内无「真残留需修」，全部命中均为可接受类别。**

---

## 古名清单一览（全仓库范围内）

### ✅ 真残留需修

**范围内：无。**

（说明：完成任务目标「改名方案」的子项可直接视为已达标，唯一的不一致是 ld-r 命名差异 D1，属命名统一问题而非古名残留。）

### ➖ 可接受（已标注类别）

| 文件 | 行 | 命中词 | 类别 |
|------|----|--------|------|
| scripts/kanban_update.py | L100-103 | 礼部/户部/兵部/刑部/工部/吏部/中书省/门下省/尚书省/太子/钦天监 | ORG_LEGACY_MAP 兼容映射表 |
| scripts/migrate_org_names.py | L5,12,34-68 | 太子/中书省/皇上/准奏/封驳/朝报官/钦天监/礼部尚书/户部尚书/兵部尚书/刑部尚书/工部尚书/吏部尚书/中书令/尚书令/门下省/尚书省/礼部/户部/兵部/刑部/工部/吏部/储君/军机处/六部 | 迁移工具（VALUE_MAP 权威映射：古名→现代名，用于清理 data/ 下历史数据） |
| install.sh | L611 | 封驳 | 注释（`### Fork 流程（审微封驳）` 标题） |
| install.sh | L4,539,555,575,589,673,757,870,1261,1337 | 三省六部/六部 | 体系架构系统名（`六部`均出现在「三省六部」语境） |
| dashboard/server.py | L1004-1010,1579-1581 | 皇上/太子/中书省/门下省/尚书省/吏部/礼部/兵部/刑部/工部/户部/钦天监/六部/储君/中书令/侍中/尚书令/朝报官/礼部尚书等 | ORG_LEGACY_MAP 兼容映射表 |
| *.bak-ancient-* | registry.json / agents.json 备份 | 古名 | 历史数据文件（改名前的备份，不在范围内，建议保留或归档） |

### ⚠️ 范围外围较大面积古名（信息性，非本次验收靶区）

- **README.md**（L49-314）：大量「太子/中书省/门下省/尚书省/礼部/兵部/工部/刑部/户部/吏部/钦天监·朝报官/储君/准奏/封驳/中书令/尚书令」—— README 仍在用完整古名体系描述架构，可能与新的现代名展示不一致。**注意：不在任务验收项 #6 的扫描范围（agents/ + scripts/*.py + install.sh + start.sh）内**，但若目标是全仓库现代名统一，建议主 Agent 评估是否更新 README（含顶栏 ASCII 架构图与表格，L49-76、L101、L165-175、L260-291）。
- **edict/**：backend（session_checker.py / models/task.py / api/agents.py / workers/*.py）与 frontend（dist 编译产物 + src/index.css 中 `.dt-中书省` 等 CSS 类名）含大量古名。edict 是一套独立的「三省六部流程引擎」子项目，域内术语自成体系，**不在本次「Agent 显示名 + 安装链路」范围内**。若需统一，属独立子项目改造，建议另立 spec 评估（注意 dist 为编译产物，需改源码后重新打包；CSS 类名改动需同步 JS 引用）。

---

## 校验命令执行记录

| 命令 | 结果 |
|------|------|
| `python3 -m json.tool registry.json` | ✅ 通过（纯 JSON） |
| `python3 -m json.tool agents.json` | ❌ 含 `#` 注释，需剔除注释后通过（JSON 体本身合法，11 条） |
| `bash -n install.sh` | ✅ 通过 |
| `bash -n start.sh` | ✅ 通过 |
| `bash -n scripts/add-agent.sh` | ✅ 通过 |

---

## 待主 Agent 处理清单

| 编号 | 级别 | 文件 | 建议 |
|------|------|------|------|
| D1 | 建议 | registry.json (ld-r.name) / agents.json (ld-r.name) | 统一 ld-r 名称。契约方案为「执行经理」，当前 registry 为「Ld.r」、agents.json 为「执行经理」，二者不一致。建议 registry.json 改为「执行经理」以与 agents.json 及展示一致。 |
| D2 | 轻微 | agents.json | `#` 注释使 strict JSON 校验失败；如需脚本化校验需去注释或改用 JSONC。 |
| D3 | 信息 | README.md、edict/ | 范围外古名面积大，如需全仓库统一建议另立任务（不阻塞本次验收）。 |

> 严格 JSON 校验要求：由于 agents.json 明确存在 `#` 注释（故 `python3 -m json.tool` 失败），验收项 #7 中「python3 -m json.tool registry.json agents.json」对 registry.json 通过、对 agents.json 以「剔除注释后校验」方式通过；非本次改动的功能性缺陷，注释为文件头部既有设计（已在文件内注明是参考模板）。

---

## 验收结论

**✅ 通过。** 本次「Agent 显示名修复 + GitHub 安装链路现代化」范围内改动达标：
- 现代名落地 100%（registry courtTitle、agents.json、agents/ bootstrap 标题、huizong=归藏）
- 无古名真残留（范围内）
- 契约 C-001 保持（courtId 不变、openclaw.json 结构不变、install.sh 复用/增量机制不变）
- 语法/JSON 校验通过（agents.json 例外为注释导致，附 D2）

建议主 Agent：处理 D1（ld-r 命名统一）、决定 D3（是否顺带更新 README/edict 的古名展示）。
