# 验证报告：改名全链路状态机测试

**任务ID**: JJC-20260801-005
**任务**: 最终验证-改名全链路状态机测试
**执行日期**: 2026-08-01
**执行部门**: 执行办（Ld.r / ld-r）
**关联方案**: docs/现代化改名方案.md + specs/现代化改名/{spec,plan,tasks}.md

---

## 一、验证结论（汇总）

| 验证项 | 对应检查 | 结果 |
|--------|---------|:----:|
| T029 全仓古名清零（代码层） | grep 古名 | ✅ 通过 |
| T018/T019 历史数据迁移 | 数据文件古名扫描 | ✅ 通过（本次补齐） |
| 运行时展示现代化 | /api/live-status officials | ✅ 通过 |
| 代码语法/服务健康 | py_compile + /healthz | ✅ 通过 |
| 测试脚本断言现代化 | test_final 等 3 文件 | ✅ 通过 |
| T030 门禁复核 sdd_enforce | /api/six-unity | ⚠️ 待确认（SIX_UNITY=0 过渡模式） |

---

## 二、详细验证过程

### 2.1 代码层古名清零（T029）
对 `dashboard/server.py`、`dashboard/dashboard.html`、`dashboard/court_discuss.py`、
`scripts/*.py`、`agents/*.md`、`README.md` 全仓扫描古名词（皇上/太子/中书省/门下省/尚书省/
六部/准奏/封驳/御批/军机处/储君/中书令/侍中/尚书令/朝报官/旨意）。

**结果**: 剩余命中均为**设计所需**，非用户可见残留：
- `ORG_MODERN` / `ORG_LEGACY_MAP` 映射表（server.py L1004-1008）—— 转换源，必须含古名
- `ORG_TO_ID` 反向映射（server.py L1580-1581、kanban_update.py L102）—— ID 反查需要
- `migrate_org_names.py` 迁移脚本本身 —— 定义古名
- 注释中的"三省六部"框架名 —— 非叙事文案

**用户可见名（皇上/准奏/封驳/御批/军机处/旨意看板/太子巡检等）0 命中** ✅

### 2.2 历史数据迁移（T018/T019）—— 本次发现并修复
`python3 scripts/migrate_org_names.py --dry-run` 发现**数据文件仍有古名残留**：

| 文件 | 迁移前 | 迁移后 | 备份 |
|------|:------:|:------:|------|
| data/tasks_source.json | 8 | **0** | ✅ .bak-20260801-185541 |
| data/agent_config.json | 1 | **0** | ✅ .bak-20260801-185541 |
| data/audit_log.json | 15 | **0** | ✅ .bak-20260801-185541 |

**残留来源**: JJC-20260801-005 运行中审微（审议部）写入的 progress_log/now 字段仍用古名
（"状态在Assigned(尚书省)"、"方案已准奏"）。执行实际迁移后全部清零。

> 手动额外备份：`tasks_source.json.pre-migrate-manual` 等 3 个，可回滚。

### 2.3 运行时展示现代化
`/api/live-status` 的 officials 显示名已全部现代化：
`taizi→总办(项目总控)`、`zhongshu→规划部(规划师)`、`menxia→审议部(审议官)`、
`shangshu→执行办(执行经理)`、`bingbu→开发部`、`xingbu→质控部`、`gongbu→设计部`、
`libu→内容部`、`hubu→交付汇总处`、`libu_hr→人力路由处`、`zaochao→运维组` ✅

### 2.4 服务健康 + 语法
- 服务运行中：`python3 dashboard/server.py`（pid 464477，端口 7891）
- `/healthz` → `{"status":"ok","checks":{...全部 true}}`
- 6 个核心 py 文件 `py_compile` 全部通过

---

## 三、遗留 / 待确认

### 3.1 `sdd_enforce: false`（T030 门禁复核）
`/api/six-unity` 返回 `sdd_enforce: false`，根因：**服务以 `SIX_UNITY=0`（默认过渡模式）启动**，
未设置环境变量。spec FR-012/SC-004 要求 `SIX_UNITY=1` 时 `sdd_enforce=true`、七维分解 ≥98。

**建议**: 若需启用拦截门禁并复核 sdd_enforce=true，需以 `SIX_UNITY=1` 重启服务。当前未重启
（避免影响运行中的看板），**待老板拍板**是否切换拦截模式。

### 3.2 live_status.json 为运行时快照
迁移未含 live_status.json（运行时由 tasks_source 重新生成）。服务重启后自动同步现代名。

### 3.3 代码未提交
改名实施涉及 19 个文件（+2367/-670）在工作区未提交，含新增 migrate_org_names.py、specs/、方案文档。

---

## 四、可回滚性
- 数据迁移：`.bak-20260801-185541`（脚本自动）+ `.pre-migrate-manual`（手动）双备份
- 代码：git 工作区未 commit，可 revert 或选择性提交

## 五、文件清单
```
改动（验证过程补充）:
  data/tasks_source.json   (迁移: 8古名→0)
  data/agent_config.json   (迁移: 1古名→0)
  data/audit_log.json      (迁移: 15古名→0)
新增（本报告）:
  specs/现代化改名/验证报告-JJC-20260801-005.md
```
