# 质控验收报告 · GitHub 安装链路现代化 + Agent 显示名修复

- **验收方**: 品控官（shencha）质控子 Agent
- **日期**: 2026-08-01
- **范围**: `/home/chee/Projects/oc-macs` 只读复查，未修改任何文件
- **任务**: 对「GitHub 安装链路现代化 + Agent 显示名修复」做独立复查验收
- **方案 A 现代名基准**: main=鮱澄、ld-r=调度长、guihua=军师、shenyi=审计官、paifa=路由主管、wenan=内容主管、daima=研发主管、sheji=视觉主管、shencha=品控官、huizong=交付主管、rongcui=运维主管

---

## 验收结论总览

| 验收项 | 结果 | 证据 |
|--------|------|------|
| 1. install.sh 古名清零（含 write_main_agenda 硬编码模板） | **PASS** | 23 个古名词均计数 0；模板用方案 A + id 保留 |
| 2. 安装脚本族古名清零 | **PASS** | 4 脚本 0 命中；全仓库 `.sh` 0 命中 |
| 3. registry.json vs agents.json 一致性 | **PASS** | 11 Agent id/name 完全一致 |
| 4. register_agents() 增量复用机制 | **PASS** | L1516 existing_ids 判断完整 |
| 5. 语法校验 | **PASS** | 5 脚本 bash -n 全过；JSON 校验通过 |
| 6. 安装即用复盘报告 | **PASS**（含 1 处文案瑕疵）| 报告存在、结论合理 |

**总评定: 全部通过 ✅**，无功能性 FAIL。发现 2 处纯文档级瑕疵（不阻塞验收）。

---

## 验收项 1：install.sh 古名清零复查 — ✅ PASS

对 `install.sh`（1910 行）逐个 grep 24 个古名（筹微/审微/镜衡/溶萃/封驳/驿使/墨卿/绘象/归藏/锋铸/执行经理/太子/中书/尚书/门下/兵部/工部/刑部/礼部/吏部/户部/钦天/准奏/皇上），**全部计数 = 0**，无任何命中。

可接受类别核对：
- 「三省六部」「三省」「六部」作为**系统架构名**保留（L4、L539-541、L575、L589、L673 等）——属于可接受类别「系统名」，非 Agent 显示名，✓ 合规。
- 全仓库古名命中**仅存在于 `.bak-*` 历史备份文件**（`install.sh.bak-ancient-*`、`agents/*.bak-name-*` 等 20 个备份）——属于可接受类别「历史备份」，✓ 合规。

### write_main_agenda 硬编码模板（L514-650）专项复查 — ✅ PASS

决策门 / 降级策略 / 三省流程 / Fork 说明区已全部改为方案 A 名，括号内 id 未丢失：

| 位置（模板内 offset） | 内容 | 判定 |
|----------------------|------|------|
| 降级策略表 | 「军师超时 10min」「审计官连续打回 3 次」「品控官验收超时」 | ✅ 方案 A |
| 三省流程 L578-583 | `军师(guihua) 起草` → `审计官(shenyi) 审核` → 执行 daima/wenan/sheji → `验收 shencha` | ✅ 方案 A + id 保留 |
| Fork 流程（审计官驳回）| `军师…sessions_send…` `sessions_spawn(shenyi) 复审` | ✅ 方案 A + id 保留 |

- 匹配到方案 A 词频：军师×6、审计官×7、品控官×1、驳回×1、运维主管×1。
- 调到 L578-583 括号 id：`guihua`、`shenyi`、`shencha` 均在位，无丢失。
- 被 F1 声称已修复的 5 个古名（筹微/审微/镜衡/溶萃/封驳）在 install.sh 中**确认清零**（各计数 0），修复落地属实。

## 验收项 2：安装脚本族扫描 — ✅ PASS

- `start.sh`(129L)、`install-service.sh`(128L)、`uninstall.sh`(402L)、`edict.sh`(241L)：对全部 24 古名 grep，**均 0 命中**。
- 扩查全仓库所有 `.sh`（含 `lib/`、`scripts/` 等）：**0 命中**。
- `start.sh` 含 `ensure_agents_registered()`（L42-80）启动自愈 + `existing_ids`（L55），与复盘 F 节描述一致。

## 验收项 3：registry.json vs agents.json 一致性 — ✅ PASS

Python 比对结果：
- 两个文件均 11 个 Agent，**id 集合完全一致**（无独有）。
- **name 字段 11/11 全部一致**：鲍澄/鮱澄 对齐，ld-r=调度长 对齐，其余 9 个（军师/审计官/路由主管/内容主管/研发主管/视觉主管/品控官/交付主管/运维主管）全部对齐。
- 古名残留扫描：两文件 **0 命中**。
- 部门维度 courtTitle 与基准完全匹配（总办/执行办/规划部/审议部/人力路由处/内容部/开发部/设计部/质控部/交付汇总处/运维组，11/11 ✅）。
- `courtId` 保留系统维度 id（taizi/zhongshu/menxia/libu/bingbu/gongbu/xingbu/hubu/zaochao 等）作架构标识，非显示名，合规。

## 验收项 4：register_agents() 增量复用机制 — ✅ PASS

`install.sh` `register_agents()`（L1491 起）确认存在并完整：
- **L1516**: `existing_ids = {a['id'] for a in agents_list}` — 读取当前 openclaw.json 已配置 id。
- **L1522-1523**: `if ag_id not in existing_ids:` → 新增写入 `'name'` + `'identity': {'name', 'emoji'}`。
- **L1534-1535**: `else: print(f'~ 已存在: {ag_id}（跳过）')` — 复用不覆盖。
- 数据源 registry.json（方案 A 唯一真源），且调用前先备份 openclaw.json（L1494-1495）。

## 验收项 5：语法校验 — ✅ PASS

```
bash -n install.sh                  → OK
bash -n start.sh                    → OK
bash -n install-service.sh          → OK
bash -n uninstall.sh                → OK
bash -n edict.sh                    → OK
python3 -m json.tool registry.json  → OK
agents.json（剥离 # 注释后）         → OK
```

## 验收项 6：安装即用复盘报告 — ✅ PASS（含 1 处文案瑕疵）

- 路径 `specs/agent-displayname-install/安装即用复盘.md` **存在**（5370 字节）。
- 结论合理：判定「安装即用整体可用」——复用现有 Agent（existing_ids 跳过）、缺失自动创建（含 name/identity.name/emoji）、start.sh 启动自愈、脚本层古名清零。F1/F2 记录与本次实际复查吻合（F1 声称修复的古名已确认清零）。
- 前提核对：复盘 L88 所述「registry/agents 已人工一致」经本次实测确认成立。

---

## 发现的非阻塞瑕疵（建议修复，不构成 FAIL）

1. **🟡 文档级**：`安装即用复盘.md` L98「由质控子 Agent（**镜衡**）独立复验」使用古名「镜衡」，未随方案 A 改为「品控官/shencha」。仅文案，不影响脚本功能。建议把 L98 与 L51 中的古名描述统一加「历史残留」标注或替换。
2. **🟡 文档级**：`安装即用复盘.md` F1 节（L51）记录的「残留」为**修复历史描述**（记录曾存在现已修复），可接受；但因被基线归为可接受类别（注释/备份/兼容映射/历史描述），无需改动，仅标注备查。

以上 2 处均为文档叙事瑕疵，实际**产品/脚本/配置层 0 残留**，验收通过。

---

## 最终裁断

**验收结论: ✅ 通过（PASS）**

GitHub 安装链路现代化 + Agent 显示名修复符合验收标准：
- 古名在安装执行文件、子 Agent 源文件、配置 JSON 中**全部清零**；
- 方案 A 现代名在 registry.json（真源）与 agents.json 完全一致；
- register_agents 增量复用机制完整，缺失自动创建、已存在复用；
- 语法与 JSON 全部校验通过；
- 复盘报告存在且结论合理。

无需要打回的硬性问题，可交付。
